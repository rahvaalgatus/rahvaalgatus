extends ../layout
include ../_mixins

block subpage
	section.primary-section.text-section: center
		h2= t("CO_AUTHORS")
		p= t("CO_AUTHORS_TEXT")

		if error
			p.flash.error= error
		
		+form.form(method="post", action="/initiatives/#{initiative.id}/authors")
			label.form-label= t("LBL_EMAIL")
			input.form-input(
				type="email",
				name="email",
				value=attrs.email,
				required)

			button.form-submit.primary-button= t("ADD_CO_AUTHOR")
